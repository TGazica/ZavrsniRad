<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.mainActivity.MainActivity">

    <FrameLayout
        android:id="@+id/navigationFrameLayout"
        android:layout_width="140dp"
        android:layout_height="match_parent"
        app:layout_constraintStart_toStartOf="parent"
        />

    <FrameLayout
        android:id="@+id/menuFrameLayout"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_toEndOf="@id/navigationFrameLayout"
        app:layout_constraintStart_toEndOf="@id/navigationFrameLayout"
        app:layout_constraintEnd_toStartOf="@id/orderFrameLayout"/>

    <FrameLayout
        android:id="@+id/orderFrameLayout"
        android:layout_width="250dp"
        android:layout_height="match_parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</android.support.constraint.ConstraintLayout>